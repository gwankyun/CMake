else
----

开始if块的else部分。

.. code-block:: cmake

  else([<condition>])

请参阅\ :command:`if`\ 命令。

The optional ``<condition>`` argument is supported for backward compatibility
only and is not evaluated.
